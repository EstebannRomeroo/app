# Getting Started

# Instrucciones:

- Instalar ASDF
- Correr:

```shell
asdf install
```

- Instalar OpenMP:

```shell
brew install llvm
brew install libomp
```

- Instalar Dependencias:

```shell
poetry install --no-root
```